<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qibla">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ManOrAutoSetup" />
        <activity android:name=".ManualSetupActivity" />
        <activity android:name=".AutomaticSetupActivity" />
        <activity
            android:name=".SetPrayerTimesActivity"
            android:label="@string/title_activity_set_prayer_times"
            android:theme="@style/AppTheme" />
        <activity android:name=".SetReciterActivity" />
        <activity android:name=".MainPrayerTimesPageActivity" />
        <activity android:name=".PrayerTimesActivity" />
        <activity android:name=".QiblaCompassActivity"></activity>

    </application>

</manifest>